# Included from HOMEASSISTANT_CONFIG/configuration.yaml as `lovelace: !include
# dashboards/lovelace.yaml`. YAML-mode dashboards would be declared here, but
# none are: Home was one until 2026-09-24 and moved to storage mode so it can
# be edited in the UI (see ../ui_dashboard.py). Kept, empty, so the include
# and the bind mount stay valid; declaring a dashboard here needs an HA restart.
dashboards: {}
